import { PedidoUSUARIOPipe } from './pedido-usuario.pipe';

describe('PedidoUSUARIOPipe', () => {
  it('create an instance', () => {
    const pipe = new PedidoUSUARIOPipe();
    expect(pipe).toBeTruthy();
  });
});
